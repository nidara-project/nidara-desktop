# 2026-03-14 - Daily Memory

## Session Start
- **Context:** Angel is having trouble accessing the previous conversation (`68ea28c5-0889-42d3-89f7-4ab91727b1d6`).
- **Discovery:** I have successfully retrieved the artifacts and logs from the previous session. 
- **Summary of Previous State:** We finalized the Hyprglass fork integration (branch `feat/6`), synced the "Aggressive Liquid Mask" logic, and committed the CC layout refinements to the Distroia repo.
- **Current Objective:** Re-establish continuity and continue with CrystalShell/DistroIA development.

## Crisis de Sesión y Recuperación de Historia
- **Evento:** Crash crítico de Hyprland (crash en `libgallium` / driver Mesa) tras sesión de optimización de Hyprglass.
- **Diagnóstico de Historia:** Se ha identificado una fragmentación en el historial. Las sesiones `1b88080a` (Hoy) y `68ea28c5` (11 Marzo) están vinculadas al **corpus `aalonx/hyprglass`** porque se iniciaron con archivos del fork activos. Las sesiones anteriores (Marzo 8 y anteriores) pertenecen al **corpus `aalonx/Distroia`**.
- **Acción:** Para ver las conversaciones "desaparecidas", el usuario debe enfocar un archivo dentro de `.hyprglass_pure_fork/`. Para la continuidad total, el agente (yo) mantendrá ambos contextos unidos mediante este archivo de memoria.

## Rescate de Sesión e Historia (Post-Crash)
- **Investigación de Historia:** Las conversaciones "desaparecidas" (como `1b88080a` y `68ea28c5`) estaban físicamente en disco pero el crasheo impidió que Antigravity escribiera su índice en `~/.gemini/antigravity/annotations/`.
- **Restauración:** He ejecutado un script de recuperación (`restore_history.sh`) que ha reconstruido los archivos `.pbtxt` para 12 sesiones perdidas. La visibilidad ha sido restaurada.
- **Post-Mortem de Errores:** Los `ConnectError` vistos eran causados por la rotura de los sockets IPC durante el crash de Hyprland. Se han limpiado los estados corruptos.
- **Estabilidad Hyprglass:**
    - Fix de Invisibilidad: Proyección simplificada a `translate(-x, -y)`.
    - Fix de Crash GPU: Implementado guardado y restauración estricta de `GL_SCISSOR_TEST` en `GlassRenderer` y `GlassLayerSurface`.
    - Buffer Security: Se mantienen los `std::clamp` y `try-catch` para asignación de VRAM.

## Technical Progress
- **Dock Evolution:** Optimized the dock window height in `DockPhysics.ts`, making it dynamic (~145-150px) to prevent excessive window size while maintaining space for icon animations.
- **Hyprglass Performance Audit:** Identified a bottleneck in the Hyprglass plugin related to per-window monitor-sized framebuffer allocation.
- **Stability Reversion:** Reverted experimental C++ optimizations for Hyprglass (shared buffers) because they interfered with Hyprland's deferred render pass, causing artifacts. Stability is restored.
- **Community Outreach:** Angel has contacted the Hyprglass creator to discuss regional effect applications and potential upstream optimizations.

## Design Decision
- **Islands over Unity:** We maintain the "ventana-isla" (island-window) architecture for the Control Center widgets to preserve individual glass lensing/fresnel effects, even with the current GPU parpadeo (flash) cost. Performance will be solved via future plugin or compositor-native updates.
