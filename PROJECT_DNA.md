# PROJECT_DNA: Crystal Shell 🦍

> **DO NOT DELETE. THIS IS THE SEED.**

## 1. Identity & Invariants (Era AGS)
- **OS Base**: EndeavourOS (Arch Linux).
- **Core Environment**: Hyprland (Wayland) v0.53.3+.
    - *Requirement*: Use named block syntax `layerrule { name = '...' ... }` for layer effects.
- **UI Engine**: AGS v3 (TypeScript/TSX) + Gtk4LayerShell.
    - *Rationale*: Overcoming Python Gtk4 limitations (stability, layer management).
- **Visuals**: Organic Dock (Glassmorphism) & "Pill" Indicators.
- **Result**: A premium, "out-of-box" experience that respects user themes.
- **Language**: Spanish (Español).

## 2. Prime Directives (The 3 Laws)
1. **Never Force-Push** without explicit user override. History is sacred.
2. **Respect the Monolith**: Do not split `app.ts` unless it exceeds 5000 lines.
3. **Run `verify_masterpiece.sh`** before declaring a task complete.
4. **Theme Agnostic**: Never hardcode icon names unless they are Freedesktop standard.

## 3. The Golden History (Source of Truth)
- **Golden Commit**: `4f72e9b` (Restored 24/01/2026).
- **Recovery**: If `brain/` is lost, read this file and re-index `.antigravity/` for context.

## 4. Continuity Strategy
- Artifacts in `.antigravity/` are the backup of the "brain".
- If the system is wiped, `git clone` + `./install.sh` + reading this file restores the agent's soul.
