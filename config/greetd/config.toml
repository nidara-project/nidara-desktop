# greetd configuration for Crystal Shell
# Installed to /etc/greetd/config.toml

[terminal]
vt = 1

[default_session]
command = "env HYPRLAND_CONFIG=/etc/greetd/hyprland-greeter.conf start-hyprland"
user = "greeter"
