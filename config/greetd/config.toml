# greetd configuration for Crystal Shell
# Installed to /etc/greetd/config.toml

[terminal]
vt = 1

[default_session]
command = "start-hyprland --config /etc/greetd/hyprland-greeter.conf"
user = "greeter"
