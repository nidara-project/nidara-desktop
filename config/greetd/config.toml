# greetd configuration for Nidara
# Installed to /etc/greetd/config.toml

[terminal]
vt = 1

[default_session]
command = "env HYPRLAND_CONFIG=/etc/greetd/hyprland-greeter.lua start-hyprland"
user = "greeter"
