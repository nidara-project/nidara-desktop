# greetd configuration for Crystal Shell
# Installed to /etc/greetd/config.toml

[terminal]
vt = 1

[default_session]
command = "env HYPRLAND_CMD=start-hyprland Hyprland -c /etc/greetd/hyprland-greeter.conf"
user = "greeter"
