<?xml version="1.0" encoding="UTF-8"?>
<!--
  The desktop's own settings (#573). Every schema here is read and written through
  `defineSettings` (ui/shell/core/configFile.ts), which maps each camelCase field of a
  store's DEFAULTS to the kebab-case key of the same name below.

  ⚠️ THE TS DEFAULTS AND THESE DEFAULTS MUST AGREE. `defineSettings` compares them at
  start and logs a JS error on drift, which fails the headless smoke. A default that
  is COMPUTED at run time (recording's `hardware` and `save-dir`) is declared in the
  store's `computed` list instead; its default here is a placeholder that nothing
  reads, and the store answers with the computed value while the user has not set
  one.

  Ranges and choices are the same bounds as the store's validators. They are here as
  well so that `gsettings set` from a terminal refuses what the shell would refuse,
  instead of accepting it and leaving the store to fall back silently.

  Installed to /usr/share/glib-2.0/schemas by the PKGBUILD (glib2's pacman hook
  compiles it) and by install.sh in both modes. Editing this file changes nothing
  until it is installed and compiled again.
-->
<schemalist>

  <schema id="org.nidara.notifications" path="/org/nidara/notifications/">
    <key name="popup-timeout" type="i">
      <default>6</default>
      <summary>Seconds a notification banner stays on screen</summary>
    </key>
    <key name="do-not-disturb" type="b">
      <default>false</default>
      <summary>Suppress banners; notifications still reach the notification centre</summary>
    </key>
  </schema>

  <schema id="org.nidara.night-light" path="/org/nidara/night-light/">
    <key name="enabled" type="b">
      <default>false</default>
      <summary>Night light is on</summary>
    </key>
    <key name="temperature" type="i">
      <range min="2700" max="6500"/>
      <default>4000</default>
      <summary>Colour temperature in kelvin</summary>
    </key>
    <key name="schedule-enabled" type="b">
      <default>false</default>
      <summary>Turn night light on and off on a schedule</summary>
    </key>
    <key name="schedule-from" type="s">
      <default>"20:00"</default>
      <summary>Schedule start, HH:MM</summary>
    </key>
    <key name="schedule-to" type="s">
      <default>"07:00"</default>
      <summary>Schedule end, HH:MM</summary>
    </key>
  </schema>

  <schema id="org.nidara.bar" path="/org/nidara/bar/">
    <key name="show-app-title" type="b">
      <default>true</default>
      <summary>Show the focused application's name in the bar</summary>
    </key>
    <key name="launcher-icon" type="s">
      <default>"nidara"</default>
      <summary>Launcher icon: a built-in name or an absolute path to an image</summary>
    </key>
  </schema>

  <schema id="org.nidara.dock" path="/org/nidara/dock/">
    <key name="icon-size" type="i">
      <range min="32" max="96"/>
      <default>64</default>
      <summary>Icon size in pixels</summary>
    </key>
    <key name="magnification" type="b">
      <default>true</default>
      <summary>Magnify icons under the pointer</summary>
    </key>
    <key name="max-icon-size" type="i">
      <range min="64" max="128"/>
      <default>128</default>
      <summary>Largest magnified icon size in pixels</summary>
    </key>
    <key name="show-indicators" type="b">
      <default>true</default>
      <summary>Show a dot under running applications</summary>
    </key>
    <key name="screen-gap" type="i">
      <range min="4" max="32"/>
      <default>8</default>
      <summary>Gap between the dock and the screen edge in pixels</summary>
    </key>
    <key name="auto-hide" type="b">
      <default>false</default>
      <summary>Hide the dock until the pointer reaches its edge</summary>
    </key>
    <key name="hide-delay" type="i">
      <range min="0" max="2000"/>
      <default>500</default>
      <summary>Milliseconds before an auto-hidden dock hides</summary>
    </key>
    <key name="position" type="s">
      <choices>
        <choice value="bottom"/>
        <choice value="left"/>
        <choice value="right"/>
      </choices>
      <default>"bottom"</default>
      <summary>Screen edge the dock sits on</summary>
    </key>
    <key name="pinned" type="as">
      <default>['nidara-settings', 'kitty', 'nidara-installer']</default>
      <summary>Desktop ids pinned to the dock, in dock order. An id that is not installed is skipped (and pruned)</summary>
    </key>
  </schema>

  <schema id="org.nidara.workspaces" path="/org/nidara/workspaces/">
    <key name="default-mode" type="s">
      <choices>
        <choice value="floating"/>
        <choice value="tiling"/>
      </choices>
      <default>"floating"</default>
      <summary>Window mode of a workspace with no override</summary>
    </key>
    <key name="workspaces" type="a{ss}">
      <default>{}</default>
      <summary>Per-workspace mode overrides: workspace number to "floating" or "tiling"</summary>
    </key>
  </schema>

  <schema id="org.nidara.recording" path="/org/nidara/recording/">
    <key name="audio-source" type="s">
      <default>"@system"</default>
      <summary>Audio source: @system, @mic, or a PulseAudio source name</summary>
    </key>
    <key name="quality" type="s">
      <choices>
        <choice value="low"/>
        <choice value="balanced"/>
        <choice value="high"/>
      </choices>
      <default>"balanced"</default>
      <summary>Encoding quality</summary>
    </key>
    <key name="framerate" type="i">
      <range min="0" max="240"/>
      <default>0</default>
      <summary>Frames per second; 0 follows the compositor</summary>
    </key>
    <key name="hardware" type="b">
      <default>false</default>
      <summary>Encode on the GPU. Unset: on when a render node exists</summary>
    </key>
    <key name="format" type="s">
      <choices>
        <choice value="mp4"/>
        <choice value="mkv"/>
        <choice value="webm"/>
      </choices>
      <default>"mp4"</default>
      <summary>Container format</summary>
    </key>
    <key name="save-dir" type="s">
      <default>""</default>
      <summary>Folder recordings are saved to. Unset: the XDG videos folder</summary>
    </key>
  </schema>

  <!--
    The Assistant and every agent client. The allow-* keys are GATES: read by the
    shell, by bin/nidara-agent every turn, by bin/nidara-mcp and by the four
    computer-use helpers (nidara-a11y/act/click/type), each of which refuses to act
    when its gate is off. A helper that cannot find this schema treats the gate as
    it used to treat a missing ai.json: computer use and control OFF, MCP ON.
  -->
  <schema id="org.nidara.ai" path="/org/nidara/ai/">
    <key name="allow-config-write" type="b">
      <default>true</default>
      <summary>Agents may change desktop settings</summary>
    </key>
    <key name="allow-screenshot" type="b">
      <default>true</default>
      <summary>Agents may take screenshots</summary>
    </key>
    <key name="allow-window-close" type="b">
      <default>true</default>
      <summary>Agents may close windows</summary>
    </key>
    <key name="allow-mcp" type="b">
      <default>true</default>
      <summary>The MCP server answers</summary>
    </key>
    <key name="allow-computer-use" type="b">
      <default>false</default>
      <summary>Agents may see other applications (AT-SPI)</summary>
    </key>
    <key name="allow-computer-control" type="b">
      <default>false</default>
      <summary>Agents may act on other applications. Requires allow-computer-use</summary>
    </key>
    <key name="allow-file-read" type="b">
      <default>true</default>
      <summary>The Assistant may read files in its read roots</summary>
    </key>
    <key name="allow-file-write" type="b">
      <default>false</default>
      <summary>The Assistant may edit its three writable files. Requires allow-file-read</summary>
    </key>
    <key name="brain-provider" type="s">
      <default>""</default>
      <summary>Assistant provider id; empty = off</summary>
    </key>
    <key name="brain-backend" type="s">
      <choices>
        <choice value=""/>
        <choice value="anthropic"/>
        <choice value="gemini"/>
        <choice value="openai"/>
      </choices>
      <default>""</default>
      <summary>Wire protocol, derived from the provider</summary>
    </key>
    <key name="brain-model" type="s">
      <default>""</default>
      <summary>Model id of the active provider</summary>
    </key>
    <key name="brain-endpoint" type="s">
      <default>"http://localhost:11434/v1"</default>
      <summary>Base URL of the active provider</summary>
    </key>
    <key name="brain-models" type="a{ss}">
      <default>{}</default>
      <summary>Last model used, per provider id</summary>
    </key>
    <key name="brain-endpoints" type="a{ss}">
      <default>{}</default>
      <summary>Last endpoint used, per provider id</summary>
    </key>
    <key name="assistant-glow" type="b">
      <default>true</default>
      <summary>Glow the focused window's border while the Assistant works</summary>
    </key>
  </schema>

  <!--
    Game mode. The compositor needs these too and cannot read dconf: the shell
    writes them into ~/.config/nidara/nidara-gaming.lua and pushes them live
    (core/GamingSync.ts); config/hypr/hyprland.lua reads NIDARA_GAMING.
  -->
  <schema id="org.nidara.gaming" path="/org/nidara/gaming/">
    <key name="wallpaper-mode" type="s">
      <choices>
        <choice value="artwork"/>
        <choice value="custom"/>
        <choice value="none"/>
      </choices>
      <default>"artwork"</default>
      <summary>Wallpaper while a game runs</summary>
    </key>
    <key name="custom-wallpaper" type="s">
      <default>""</default>
      <summary>Image used when wallpaper-mode is custom</summary>
    </key>
    <key name="transition" type="s">
      <choices>
        <choice value="random"/>
        <choice value="simple"/>
        <choice value="fade"/>
        <choice value="center"/>
        <choice value="grow"/>
        <choice value="outer"/>
        <choice value="wipe"/>
        <choice value="wave"/>
        <choice value="left"/>
        <choice value="right"/>
        <choice value="top"/>
        <choice value="bottom"/>
      </choices>
      <default>"grow"</default>
      <summary>Wallpaper transition into and out of game mode</summary>
    </key>
    <key name="performance-profile" type="b">
      <default>false</default>
      <summary>Switch the power profile to performance while a game runs</summary>
    </key>
  </schema>

  <!--
    The appearance keys only Nidara has (#573). The ones the desktop standard
    already names — accent, colour scheme, GTK/icon/cursor theme, fonts — live in
    org.gnome.desktop.interface, where every app reads them. Applications (and our
    own installer and lock screen) never read THIS schema: bin/nidara-portal serves
    it as the org.nidara.appearance portal namespace, under these same key names,
    and the shell writes the greeter's mirror from it. Ranges = GLASS_RANGE
    (ui/lib/theme-tokens.ts).
  -->
  <schema id="org.nidara.appearance" path="/org/nidara/appearance/">
    <key name="bar-opacity" type="d">
      <range min="0.24" max="0.8"/>
      <default>0.48</default>
      <summary>Glass opacity of the bar</summary>
    </key>
    <key name="overlay-opacity" type="d">
      <range min="0.24" max="0.8"/>
      <default>0.48</default>
      <summary>Glass opacity of the panels that open from the bar</summary>
    </key>
    <key name="dock-opacity" type="d">
      <range min="0.24" max="0.8"/>
      <default>0.24</default>
      <summary>Glass opacity of the dock</summary>
    </key>
    <key name="window-opacity" type="d">
      <range min="0.24" max="0.8"/>
      <default>0.48</default>
      <summary>Glass opacity of Nidara's own windows</summary>
    </key>
    <key name="shell-appearance" type="s">
      <choices>
        <choice value="system"/>
        <choice value="dark"/>
        <choice value="light"/>
      </choices>
      <default>"system"</default>
      <summary>The shell's own skin: follow the system mode, or pin it dark or light</summary>
    </key>
  </schema>

  <!--
    Which widgets show where. Keyed by widget id — the keys ARE the data, so the
    map only holds what differs from, or was saved over, the code's first-run
    placement (DEFAULT_PLACEMENT in ui/shell/widgets/index.ts, merged underneath).
  -->
  <schema id="org.nidara.widgets" path="/org/nidara/widgets/">
    <key name="placement" type="a{s(bb)}">
      <default>{}</default>
      <summary>Widget id to (shown in the bar, shown in the Control Centre)</summary>
    </key>
  </schema>

  <!-- The Control Centre grid. Empty = the default layout (CC_DEFAULT_ORDER). -->
  <schema id="org.nidara.control-center" path="/org/nidara/control-center/">
    <key name="positions" type="a{s(ii)}">
      <default>{}</default>
      <summary>Widget id to its grid cell (column, row)</summary>
    </key>
    <key name="sizes" type="a{ss}">
      <default>{}</default>
      <summary>Widget id to its footprint ("1x1", "2x1", "1x2", "2x2", "4x1") where it is not the widget's default</summary>
    </key>
  </schema>

  <!--
    How the clock reads. NOT the timezone (the system's, set through timedatectl)
    and NOT the regional format (LC_* in ~/.config/environment.d/nidara-locale.conf,
    read at login): each of those already has a home, and a copy here was a second
    one. The shell mirrors these three to /var/tmp/nidara/region.json for the greeter.
  -->
  <schema id="org.nidara.region" path="/org/nidara/region/">
    <key name="time-format" type="s">
      <choices>
        <choice value="24h"/>
        <choice value="12h"/>
      </choices>
      <default>"24h"</default>
      <summary>24- or 12-hour clock</summary>
    </key>
    <key name="date-format" type="s">
      <choices>
        <choice value="none"/>
        <choice value="short"/>
        <choice value="short-year"/>
        <choice value="long"/>
        <choice value="numeric"/>
        <choice value="iso"/>
      </choices>
      <default>"long"</default>
      <summary>Date shown beside the clock in the bar</summary>
    </key>
    <key name="show-seconds" type="b">
      <default>false</default>
      <summary>Show seconds in the clock</summary>
    </key>
  </schema>

</schemalist>
