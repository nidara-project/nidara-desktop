# Nidara assets

Orphan branch holding binary assets referenced from the main branch (README
screenshots, promo material). Kept out of `main` so cloning the code stays lean.

- `screenshots/` — README screenshots, 1920×1080 WebP (q92). Captured in the
  QEMU staging VM (snapshot `shots-studio-07-03`); originals kept by the maintainer.
