{
  "scripts": {
    "predev": "node ../../scripts/gen-widget-index.mjs",
    "dev": "sass --no-charset style.scss style.css && sed -i '/@charset/d' style.css && ../../scripts/run.sh app.ts",
    "prerun": "node ../../scripts/gen-widget-index.mjs",
    "run": "../../scripts/run.sh app.ts",
    "pretypecheck": "[ -d @girs ] || ../../scripts/gen-types.sh .",
    "typecheck": "npx --yes -p typescript@5.7.3 tsc --noEmit -p tsconfig.json",
    "prebuild": "node ../../scripts/gen-widget-index.mjs",
    "build": "mkdir -p build && sass --no-charset style.scss style.css && sed -i '/@charset/d' style.css && ../../scripts/bundle.sh app.ts build/nidara"
  },
  "dependencies": {
    "sass": "^1.97.3"
  },
  "prettier": {
    "semi": false,
    "tabWidth": 2
  },
  "devDependencies": {
    "lucide-static": "^1.11.0"
  }
}
