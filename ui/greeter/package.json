{
  "scripts": {
    "dev": "cd ../shell && npx sass --no-charset ../greeter/style.scss ../greeter/style.css && sed -i '/@charset/d' ../greeter/style.css && cd ../greeter && ags run --gtk 4 app.ts",
    "build": "mkdir -p build && cd ../shell && npx sass --no-charset ../greeter/style.scss ../greeter/style.css && sed -i '/@charset/d' ../greeter/style.css && cd ../greeter && ags bundle --gtk 4 app.ts build/nidara-greeter"
  },
  "prettier": {
    "semi": false,
    "tabWidth": 2
  }
}
