{
  "scripts": {
    "dev": "sass style.scss style.css && ags run app.ts",
    "run": "ags run app.ts",
    "build": "mkdir -p build && sass style.scss style.css && ags bundle app.ts build/distroia"
  },
  "dependencies": {
    "ags": "*",
    "gnim": "*",
    "sass": "^1.97.3"
  },
  "prettier": {
    "semi": false,
    "tabWidth": 2
  }
}