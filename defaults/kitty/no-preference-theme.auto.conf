# Used when the system reports no color-scheme preference (e.g. before the
# shell has written one). Convention (see the kitty themes kitten docs):
# no-preference means "not dark", so use the light colors.
include light-theme.auto.conf
