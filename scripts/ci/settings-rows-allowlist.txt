# Settings rows allowlist for settings-config-contract.mjs
# Format:
#   <Page.tsx>:<i18n-key-or-line> # unmigrated setting row
#   <Page.tsx>:gtk-switch:<count> # legitimate per-object toggles (#341)
#
# Every declarative setting row in Settings pages has now been migrated to settingRow() (S3b).
# Only legitimate per-object switches (not scalar settings) remain below.

# Legitimate per-object switches (not scalar settings - see #341)
Autostart.tsx:gtk-switch:1 # per-app autostart toggle
Bluetooth.tsx:gtk-switch:1 # adapter power toggle
Network.tsx:gtk-switch:1 # wifi adapter toggle
Users.tsx:gtk-switch:2 # user admin toggles (create dialog + user edit row)
Widgets.tsx:gtk-switch:1 # per-widget placement toggle
