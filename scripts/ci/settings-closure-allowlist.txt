# settings-closure-check.mjs — forbidden modules Settings still reaches (#571).
# This file may only SHRINK. Run `node scripts/ci/settings-closure-check.mjs --print`
# for the import chain to each one; cut the first edge, then delete the line.
core/NetworkAgent.ts
core/Status.ts
